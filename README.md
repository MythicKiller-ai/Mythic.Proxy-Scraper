# How To Use:
Must have python installed
open cmd in the folder of Proxy Scraper (type cmd and press enter)
then type py -m pip install -r requirements.txt
after that wait until installing finishes 
then type py Proxyscraper.py and there you go
